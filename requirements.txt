# AI-Translation requirements
# Install with: pip install -r requirements.txt
#
# For GPU support (RTX 3050), install PyTorch with CUDA 12.4 first:
#   pip install "torch>=2.6.0" --index-url https://download.pytorch.org/whl/cu124
# Then install the rest:
#   pip install -r requirements.txt
#
# Note: torch>=2.6.0 is required due to CVE-2025-32434 (torch.load safety fix).
# CUDA 12.4 wheels support 2.6+; CUDA 12.1 wheels only go up to 2.5.1.

# Web server
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9  # required for file uploads (test-file feature)

# Audio capture
sounddevice>=0.4.6
numpy>=1.26.0

# VAD (Silero is fetched via torch.hub — torch + torchaudio required)
torch>=2.6.0
torchaudio>=2.6.0

# STT
faster-whisper>=1.0.0

# Translation
transformers>=4.40.0
sentencepiece>=0.2.0
sacremoses>=0.1.1

# TTS (Piper Python package — optional, falls back to subprocess)
# piper-tts  # uncomment if piper is installed as a Python package

# QR code
qrcode[pil]>=7.4.2

# YAML config
pyyaml>=6.0.1

# MIDI (optional)
mido>=1.3.0
python-rtmidi>=1.5.8

# Audio utilities (optional, used for WAV manipulation if needed)
scipy>=1.13.0

# HTTP / async
httpx>=0.27.0
